# Copy to .local/production.env (gitignored) for pushing scraped listings to production.
# Fredy itself keeps running locally — only this import step talks to prod.

DATABASE_URL=postgresql://user:pass@your-prod-host:5432/renting_berlin
REDIS_URL=redis://:password@your-prod-redis:6379
